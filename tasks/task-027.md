---
id: task-027
title: 会話履歴の保持期間ポリシー実装
parents: [法的文書, バックエンド]
status: done
priority: low
depends_on: []
this_week: false
completed_at: 2026-03-02
progress: 100
note: "件数ベースの自動削除を実装。ユーザーごと最新50件を保持"
estimated_hours: 0.1
---

## 概要

チャット会話履歴がDBに無制限に蓄積する。保持期間ポリシーがない。

## リスク

- 借金額・差押状況・生活保護受給の発言が永遠に残る
- プライバシーポリシーにデータ保持期間の言及がない

## 採用した方針

期間ベースではなく**件数ベース**の自動削除を採用。

- ユーザーごとに最新50件の会話を保持
- チャット送信時に50件を超えた古い会話を自動削除
- Cronや外部サービス不要（チャットAPI内で完結）
- 暗号化（task-034）と合わせてプライバシーリスクを軽減

### 検討したが見送った案
- ユーザー主導の履歴クリアボタン: 「全データ削除」が既にあり、暗号化も済んでいるため不要と判断
- 期間ベースの自動削除: 件数ベースの方がユーザー体験が安定する（頻度に関わらず最新50件は残る）

## 実装箇所

- `app/api/chat/route.ts`: 会話保存後に50件超過分を削除する処理を追加

## 実装内容

### 変更ファイル
- `app/api/chat/route.ts` - 会話保存後に50件超過分を自動削除するロジックを追加

### 実装内容
- チャットAPI（POST）で会話をDBに保存した直後に、ユーザーごとの会話件数をチェック
- `MAX_CONVERSATIONS = 50` を超えた場合、created_at降順で50件目のタイムスタンプを取得し、それより古い会話を一括削除
- `document_id IS NULL` の条件で、書類コンテキスト付き会話は除外して件数管理
- Cronや外部サービス不要で、チャットAPI内で完結する実装
