# 🏛️ Fumuly デザインアーキテクチャ選択

*作成日：2026-02-23*

---

## 確定事項

| 項目 | 選択 | 理由 |
|------|------|------|
| フレームワーク | **Next.js** | App Router・SSR・PWA対応が充実 |
| CSSフレームワーク | **Tailwind CSS** | 今回shadcn/uiの要件として必須 |
| コンポーネントライブラリ | **shadcn/ui** | 安定性・カスタマイズ性・Next.js相性◎ |
| フォント | **Noto Sans JP + Inter** | Google Fonts・無料・読みやすさ重視 |

---

## 各選択の詳細

### Next.js

- App Router（Next.js 13以降）を使用
- PWA化には`next-pwa`または`@ducanh2912/next-pwa`を使用
- Renderへのデプロイは`output: 'standalone'`設定で対応

```javascript
// next.config.js
const withPWA = require('@ducanh2912/next-pwa').default({
  dest: 'public',
  cacheOnFrontEndNav: true,
  aggressiveFrontEndNavCaching: true,
});

module.exports = withPWA({
  output: 'standalone',
});
```

### shadcn/ui + Tailwind CSS

- shadcn/uiはコンポーネントをコピーして使う設計のため、カスタマイズが自由
- Fumulyのブランドカラーをshadcn/uiのCSS変数として設定する
- 使用予定コンポーネント：Card / Button / Badge / Dialog / Toast / Sheet

```javascript
// tailwind.config.js にブランドカラーを定義
module.exports = {
  theme: {
    extend: {
      colors: {
        primary:  '#2C4A7C',
        accent:   '#F4845F',
        urgent:   '#E05252',
        action:   '#F0A500',
        keep:     '#52A06E',
        ignore:   '#ABABAB',
      }
    }
  }
}
```

---

## 将来的な検討（v1.0以降）

### Liftkit

黄金比ベースで余白・フォント・ボーダーラジウスを自動計算するUIフレームワーク。Next.jsとの相性が良く、Fumulyのシンプル・クリーンなトーンと合っている。

**現時点での見送り理由：**
- まだアルファ版でドキュメントが整備途中
- メンテナーが2人のみでリスクあり
- MVP段階では安定性を優先

**v1.0デザインリニューアル時に再評価する。**

---

## ディレクトリ構成（案）

```
fumuly/
├── app/
│   ├── (auth)/
│   │   ├── login/
│   │   └── register/
│   ├── (main)/
│   │   ├── home/
│   │   ├── scan/
│   │   ├── documents/
│   │   │   └── [id]/
│   │   └── settings/
│   └── api/
│       ├── analyze/       ← Claude API呼び出し
│       ├── documents/     ← CRUD
│       └── stripe/        ← Webhook
├── components/
│   ├── ui/                ← shadcn/uiコンポーネント
│   └── fumuly/            ← Fumuly独自コンポーネント
├── lib/
│   ├── supabase.ts
│   ├── claude.ts
│   └── stripe.ts
└── public/
    └── manifest.json      ← PWA設定
```

---

## 今後の検討事項

- [ ] `next-pwa` vs `@ducanh2912/next-pwa` の最終選定
- [ ] shadcn/uiのテーマ設定（CSS変数のブランドカラー適用）
- [ ] カメラ撮影UIの実装方針（`react-webcam` or ネイティブinput）
- [ ] IndexedDBのラッパーライブラリ選定（`idb` or `Dexie.js`）
- [ ] v1.0以降でのLiftkit再評価
